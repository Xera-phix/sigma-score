// Copied from template/components/ui/tooltip.tsx
import * as React from "react";

export function TooltipProvider({ children }: { children: React.ReactNode }) {
  // Minimal placeholder for TooltipProvider
  return <>{children}</>;
}
